<van-tabbar active="{{ active }}" bind:change="navigationSwitch">
    <van-tabbar-item info="3" name="pages/index/index">
      <image
        slot="icon"
        src="{{ icon.normal }}"
        mode="aspectFit"
        style="width: 30px; height: 18px;"
      />
      <image
        slot="icon-active"
        src="{{ icon.active }}"
        mode="aspectFit"
        style="width: 30px; height: 18px;"
      />
      动态
    </van-tabbar-item>
    <van-tabbar-item icon="setting-o" name="pages/user/user">我的</van-tabbar-item>
  </van-tabbar>